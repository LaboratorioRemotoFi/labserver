{
  "name": "labserver",
  "version": "0.0.1",
  "description": "Servidor de una práctica para el laboratorio remoto",
  "main": "index.js",
  "private": true,
  "workspaces": ["client"],
  "scripts": {
    "dev": "yarn dev-server && yarn dev-client",
    "dev-server": "NODE_ENV=development nodemon index.js --ignore 'client/*'",
    "dev-client": "cd ./client && yarn react-scripts start",
    "build-client": "cd ./client && yarn react-scripts build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.3.1"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^2.0.14"
  }
}
